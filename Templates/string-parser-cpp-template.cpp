#include <bits/stdc++.h>
using namespace std;

int main() {

	string line;
	
	//Reads the string until end of line
	while(getline(std::cin, line)) {
	
	        //Get string stream and separate string, word by word
		istringstream iss(line);
		do {
			string word;
			iss >> word;
 
		} while(iss);
		cout << endl;
	}

	return 0;
}
